[project]
name = "webnr2"
version = "0.1.0"
description = "FastAPI + OpenAI backend"
requires-python = ">=3.12"

dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.6",
  "pydantic>=2.0.0",
  "openai>=1.0.0",
]