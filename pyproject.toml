[project]
name = "website-backend"
version = "0.1.0"
description = "FastAPI backend for website"
requires-python = ">=3.12"

dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.6",
  "pydantic>=2.0.0",
  "openai>=1.0.0",
]